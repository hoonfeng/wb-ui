feat: gradient size/position, scrollbar-width/color, ::selection colors

Candidates round (paint/style properties, no layout changes):

1. Multi-background + size combination:
   - style/resolver.go parseBgLayerPosSize: the background shorthand now
     extracts the first layer's position/size ("linear-gradient(...) 0
     0/70px 70px no-repeat" → BackgroundPosition "0 0",
     BackgroundSize "70px 70px"). Handles both merged ("0/70px") and
     tokenizer-split ("0", "/", "70px") slash shapes.
   - rendering/backgroundimage.go computeGradientDest: explicit sizes
     confine a gradient to a sub-rect offset by position; auto/cover/
     contain fill the box (gradients have no intrinsic size).
   - rendering/painter.go paints gradient layers into that dest.
   - Probe: wb-ui draws the 70x70 block correctly (Edge headless only
     renders the top strip for sized backgrounds — its known quirk, so
     covered by unit tests instead).

2. scrollbar-width/color:
   - renderpipeline.go reads scrollbar-width (thin→8px, none→hidden but
     still scrollable) and scrollbar-color ("thumb track" overrides the
     default palette). Values already land in ComputedStyle.Properties via
     the resolver's unknown-property fallback.

3. ::selection custom colors:
   - style/resolver.go SelectionColors() walks stylesheets for
     ::selection rules (bg + color).
   - rendering/renderview.go RenderView.SetResolver/Resolver.
   - rendering/painter.go PaintSelection uses the ::selection background
     instead of the default translucent blue; dev/consistency wires the
     resolver into the render view.
   - PaintSelection default stays when no rule matches.

4. Image loading: background url() also accepts http(s) (one-shot fetch,
   10s timeout, 16MiB cap, cached by URL) alongside data: and files.

Tests: gradsize_test.go (dest math + confined gradient paint),
resolver tests (shorthand pos/size, scrollbar props, ::selection colors).
go test green. GEO unaffected (no layout changes; Edge slow locally).
