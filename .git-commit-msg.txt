feat: form control visuals + inline-block layout + transform-origin

Form controls (candidate: full UA visuals):
- layout/inlineformattingcontext.go — inline-block layout was broken for
  controls: horizontal/vertical margins ignored, definite width collapsed
  to content width by the child's IFC Layout, definite height ignored, and
  vertical-align:middle re-centered the box without its margins. Now:
  margins applied (position + line width), explicit width re-applied after
  child Layout, definite height on inline-block/replaced wins, and the
  middle alignment accounts for margins. Button geometry went from
  (8,8 81x26) to the exact Edge (18,18 120x34).
- html5/defaultcss.go — button UA style mirrors Edge's Windows button:
  #767676 border, #efefef top highlight band (gradient to 28%), white
  body, #f0f0f0 background-color (keeps GEO parity).
- style/resolver.go — `background` shorthand now resets background-image
  (author background-color must clear the UA gradient, CSS shorthand
  semantics).
- rendering/renderformcontrol.go — checked checkbox/radio use the Windows
  accent blue (RadioBgHot added; check/dot white), matching Edge.
- Pixel cases: px_button (2.3%, corner rounding), px_checkbox (3.2%,
  baseline alignment).

Background stacking: painter.go paints background-color beneath linear/
radial gradients (CSS multi-layer order).

transform-origin (candidate: more transform): TransformOriginX/Y style
fields + parseTransformOrigin (px/%/keywords) + applied in walkSubtreeExcluded
(T(origin)·ops·T(-origin)); unit test covers %/px resolution and corner
scale pivot.

Verified: GEO 13/13, pixel suite 19/19, go test green.
