feat: multi-layer backgrounds + textarea/focus UA + text input borders

Form-control & UA polish (candidates: select/textarea visuals, focus):
- html5/defaultcss.go — text inputs and textarea now use Edge's #767676
  border (was #c0c0c0); added the default :focus outline (1px #4d90fe,
  Edge/Chrome focus ring). :hover/:focus selector matching and host-driven
  hovered/focused state already existed; the UA outline was missing.
- select stays as-is: Edge headless renders native widgets blank, so pixel
  comparison is meaningless; GEO geometry already aligned.

Multi-layer backgrounds (candidate: multi-background stacking):
- style/resolver.go — the background shorthand now joins multiple gradient
  layers with commas (first layer = topmost) instead of keeping only the
  first.
- rendering/gradient.go — splitBackgroundLayers splits top-level commas
  (parenthesis-aware so gradient args stay intact).
- rendering/painter.go — paints all gradient layers bottom-up so the FIRST
  layer ends up on top (CSS background layering), with background-color
  beneath. Opaque top layer wins (px probe 1:1 with Edge); semi-transparent
  top layer blends with the layer below (unit test: rgba(255,0,0,0.5) over
  green → #807f00).
- rendering/multibg_test.go — layer splitting + opaque/blend paint tests.

Pixel cases: px_textarea (0.48%, border aligned; Edge headless selection
highlight budget). Verified GEO 13/13, pixel suite 20/20, go test green.
