fix: svg intrinsic height + placeholder color match Edge

Continuing the component-positioning sweep on the real companion
frontend (edge_compare band analysis):

1. intrinsicContentHeight now reads the height attribute for svg/img
   elements (like the width fix for intrinsicContentWidth). A 14px svg
   icon without CSS height fell back to fontLineGap (~16px), inflating
   column-flex items and blocks that contain icons.

2. Placeholder text color 0xA0A0A0 → 0x808080 to match Edge's default
   (#7F7F7F) for <input> and <textarea> placeholder painting.

Scan conclusions (no engine bugs — data/state differences):
- chat-input band: phase-bar (v-if element renders 40px without JS),
  obtn label width (font metrics), 1-2px divider lines
- conv-sidebar: cache-ring dasharray + Vue interpolation without JS
- sidebar toolbar height: fontLineGap estimate (33 vs Edge 28) — the
  alternative (intrinsicContentHeight) regresses chat-input 9.6→14.2%,
  so it stays
- activity-bar: 1px indicator offset + hover state color

GEO 13/13, full go test green.
