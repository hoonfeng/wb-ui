fix: flex intrinsic width for svg/img replaced elements

Flex items containing a 12px svg icon (width attribute, no CSS width)
measured 0 from their empty children — .obtn buttons sized ~3px under
content (38px vs Edge 53px), content overflowed into neighbors, and the
space-between send-btn was shoved 16px right past the input border.

intrinsicContentWidth now reads the width attribute for svg/img elements.
Verified vs Edge on the real companion frontend: obtn gap 2px restored,
send-btn at x=965 (was 981, overflowing by 3px), shield green-ink 6 vs
Edge 7, diff send-btn region 1291->348.

Tests: TestFlexGapBetweenItems, TestFlexObtnEstimates. GEO 13/13.
