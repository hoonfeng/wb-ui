feat: SVG path implicit command repeats + arc sampling density

Fixes the settings gear and shield icons rendering broken:

1. parseSVGPathData now splits consecutive parameter groups after a
   command into SEPARATE commands per the SVG spec ("l-8-3-8 3" is
   "l -8 -3 l -8 3"). Previously all groups were packed into one
   pathCmd but painters only read the first group:
   - shield "l-8-3-8 3": second l(-8,3) dropped → left half missing
   - settings gear "a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0": second arc
     dropped → gear teeth incomplete
   Multi-point M/m now also degrades to L/l per spec.

2. arcToPolyline sampling 8°→4° per segment: at 18px icon sizes the
   straight-line arc approximation made gear teeth visibly faceted;
   4° keeps short arcs round when scaled down (activity-bar 3.1%→3.0%).

Pixel verification vs Edge: shield band diff 0/1600, 18px gear nearly
identical (only the lower-left tooth 1px off); 24x24 unscaled render
is symmetric and structurally correct. GEO 13/13 green.

Tests: svg_implicit_test.go (shield l-repeat, gear arc-repeat,
multi-M→L), svg_gear_test.go (settings path arc sweep tally),
graphics strokewidth_test.go (Skia stroke scales with CTM).
