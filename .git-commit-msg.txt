fix: cross-platform window backend alignment (IME/drop/focus/software render)

- app/host.go: fix software-rendered backends (X11/Cocoa) never rendering —
  the main loop skipped the paint path when GPUSurface() returned nil; now
  falls back to Window.Canvas() for CPU raster + Present
- platform/window: add Canvas() to all three backends (glfw returns nil,
  x11/macos return the raster canvas)
- platform/window/window_x11.go: wire up XIM IME (Init + SetClientWindow +
  FilterEvent on KeyPress), implement IME()/PollIMEEvents()/SetIMECompositionPos/
  SetIMEEnabled, add PostEvent/Focus (XRaiseWindow), call dropCallback on drop
- platform/window/window_macos.go: wire up NSTextInputClient IME (Init with
  NSView), implement IME methods, add PostEvent/Focus (cocoa_focus helper),
  remove unused extern goCocoaViewEvent declaration
- platform/window/window_glfw.go: import graphics for Canvas()
