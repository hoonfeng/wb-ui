feat: SVG <g>/nested svg + bezier paths + object-position + SVG <image>

Candidates round:

1. SVG <g> groups + nested <svg> (svg.go + svg_group_test.go):
   - <g> fill/stroke inheritance already flowed through walk's elCtx; tests
     confirm group fill applies to children.
   - Nested <svg> x/y offsets were missing: children painted at absolute
     coords, ignoring the inner svg's x/y. walk now accumulates an (offX,
     offY) passed to descendants; shapes get wrapped in svgTranslatedShape.
     Verified: inner <svg x=30 y=20> shifts its rect to (30,20)+.

2. SVG path curves (svg.go + svg_path_test.go):
   - parseSVGPathData parsed C/c (cubic) and Q/q (quadratic) commands but
     svgPath.paint only handled M/L/Z — curves rendered as straight M/L
     segments. Added cubic/quadratic bezier sampling (16/12 steps) with
     relative-command support, plus current-point tracking.
   - Verified: cubic "M 10 50 C ... Z" fills red center; quadratic
     stroke="blue" paints along the curve apex.

3. img object-position (painter.go + objectfit_test.go):
   - cover/contain/none now respect object-position (default 50% 50%);
     percent/keyword parser (left/top/center/right/bottom). none now
     centers by default per CSS Images §3 (was top-left).
   - TestObjectFitPosition: none + left top pins the image to the corner.

4. background-position — already complete (percent/keyword/px in
   parseBackgroundPosition); no change needed.

5. SVG <image> element (svg.go + svg_group_test.go):
   - New svgImage shape: href (or xlink:href) resolves through the shared
     image cache (data/file/http), drawn scaled to x/y/width/height.
   - Verified: 10x20 red PNG data URI at (20,30) 40x40 paints red center.

Tests: group fill, nested svg offset, cubic fill, quadratic stroke,
object-fit position, SVG image. Full go test green.
