fix: flex column justify re-center + SVG path H/V + circle dash

desktop_probe (welcome_geo / icon_cmp / svg_ring / chat_region) located
three real rendering defects reported by the user:

1. Editor-area text overlap (welcome page): flex-column with
   justify-content:center re-centered EVERY auto-height item to
   cy+(ch-bh)/2 after child layout — collapsing all children onto the
   single container-center position (logo/text/sub all at y≈200; in the
   desktop app welcome-logo 261 / welcome-text 333 / welcome-sub 365 all
   overlapped in a 64px band). Fixed: remove per-item main-axis
   re-centering; for auto-height containers re-center / flex-end the whole
   group ONCE after the loop (single shift keeps stacked positions).
   welcome children now stack correctly (261 → 333 → 365 with 8px gap).

2. SVG path H/h (horizontal) and V/v (vertical) line commands were not
   parsed — paths using them (e.g. the lucide folder icon M22 19H4V5h5l2
   3h9z) drew only stray arcs: the activity-bar icon body was missing
   (85 ink px vs Edge's 218). Added H/h/V/v cases; first icon now draws
   the full folder outline (123 ink px, structure matches Edge).

3. SVG circle/ellipse stroke-dasharray was ignored — progress rings drew
   a full circle instead of the dash pattern. svgEllipse.paint now samples
   the outline (96 segments) and draws dashes with cumulative arc-length
   offset, matching path/line dash behavior.

Regression tests: TestFlexColumnCenterGap (3 stacked children, 8px gaps),
TestSVGPathHVCmds (M/H/V/h/l/z all parsed).

Full go test green; GEO 13/13 (17s).
