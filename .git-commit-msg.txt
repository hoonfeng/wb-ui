feat: position:sticky + backdrop-filter + SVG marker + file input

Gap-filling round — closes all four recorded gaps:

1. position: sticky (box.go + renderbox.go + renderpipeline.go +
   sticky_test.go):
   - Layout: IsRelativelyPositioned now includes sticky (WebKit semantics),
     so sticky participates in normal flow and applies top/left offsets like
     relative.
   - Render: walkSubtreeExcluded wraps sticky boxes in a scroll-pinning
     translate (computeStickyOffset): once the element's viewport position
     scrolls past the top line, it is pinned back to top (document-level
     scroll; nested scroll containers treated as relative — noted in the
     comment).
   - Verified: bar at static y=300 with top:10 pins to viewport y=10 after
     scrolling 400px; unscrolled stays at 300.

2. backdrop-filter (renderpipeline.go + backdrop_test.go):
   - New paintBackdrop: snapshots the current canvas, runs the CSS filter
     chain (blur etc.) offscreen over the box region via a SaveLayer with a
     filter-bearing paint, and blits the result back before the box's own
     background paints (software approximation of WebKit backdrop blur).
   - Verified: red square behind a backdrop-filter:blur(6px) box bleeds
     beyond its sharp edge inside the box region.

3. SVG <marker> (svg.go + svg_marker_test.go):
   - svgDocument collects <marker> templates (defs + top-level); svgPath
     reads marker-start/marker-end; paintSVGMarker draws the marker's child
     shapes at the path vertex, translated by refX/refY and rotated to the
     path direction when orient=auto.
   - Verified: marker-end triangle renders at the end of a horizontal line.

4. file input rendering (renderformcontrol.go + fileinput_test.go):
   - type=file now paints a native look: light-gray "选择文件" button on the
     right plus the chosen file's basename (or placeholder) on the left,
     truncated with ellipsis.

Desktop integration verification:
   - gou-ide cmd/desktop and cmd/render_test build clean with CGO.
   - dev/consistency GEO: 13/13 green (18.4s) — rendering consistency
     against Edge intact.
   - cmd/render_test shows 34/69 due to its own baseline conventions
     (transparent canvas vs black, premultiplied vs straight alpha) — not
     engine defects; a minimal probe confirmed rgba background paints
     correctly (premultiplied #80000080 = alpha 128 red).

Tests: sticky pin/no-scroll/relative, backdrop blur bleed, SVG marker,
file input paint + type mapping. Full go test green.
