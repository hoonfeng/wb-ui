test: evenodd fill-rule tests use in-hole sample points

The goskia SetFillType(evenodd) binding was suspected broken after the
bowtie/star tests "failed". Root cause: both test points (20,20) sat ON
path boundaries — the bowtie crossing vertex and the star's vertical
edge outer[0]→inner[2] (20,6)→(20,26) which passes through the center.

Fix the sample points to genuinely in-hole coordinates and restore the
strict assertions:

- graphics evenodd_test.go: bowtie hole (20,18) stays unpainted; new
  TestSkiaEvenOddStar proves the SVG star's center pentagon hole (20,23)
  is empty while the arm (20,9) fills solid 255.
- svg_enhance_test.go: TestSVGFillRuleEvenOdd now asserts the star hole
  via (20,23) and removes the debug logs.

Conclusion: evenodd works end-to-end (goskia SetFillType → graphics.
Canvas.FillPath → svg fill-rule). No engine code changed.
