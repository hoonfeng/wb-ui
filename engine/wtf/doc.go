// Package wtf provides WebKit Template Framework (Source/WTF/wtf) ported to Go.
//
// It contains core container classes, smart pointers, string abstractions,
// and platform/compiler abstractions mirroring WebKit's WTF library so that
// downstream packages (dom/css/rendering/...) can stay close to their
// upstream C++ counterparts.
package wtf
