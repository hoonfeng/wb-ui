fix: 内联空白文本节点未推进 line width

InlineFormattingContext 中纯空白文本节点
（如 </span> <span> 之间的空格）或带前导空格的
文本节点（如 " main"）的空白被跳过但不推进
line width，导致后续元素起始位置重叠。

修复：当文本节点以空白开头且当前行已有内容时，
在分词前先推进 spaceWidth，确保空白占据正确的
视觉宽度。
